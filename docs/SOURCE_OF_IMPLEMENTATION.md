# Source of Implementation

## Overview

This document maps every file in the Retail Pricing Feed Management System to its purpose, responsibilities, and relationships. It serves as the definitive reference for understanding where each feature is implemented and how the components connect.

## Project Structure

```
├── backend/                    # Node.js + Express API (TypeScript)
│   ├── prisma/
│   │   ├── schema.prisma       # Database schema (single source of truth)
│   │   ├── seed.ts             # Initial data seeding
│   │   ├── dev.db              # SQLite database file
│   │   └── migrations/         # Prisma migration history
│   ├── src/
│   │   ├── server.ts           # Application entry point
│   │   ├── controllers/        # Request handlers (business logic)
│   │   ├── routes/             # Route definitions and middleware wiring
│   │   ├── middleware/         # Auth, validation, error handling
│   │   ├── services/           # AI service, audit logging
│   │   ├── utils/              # Prisma client, Winston logger
│   │   └── validators/         # Joi validation schemas
│   ├── uploads/                # Temporary CSV file storage
│   └── logs/                   # Winston log output
├── frontend/                   # React 18 SPA (TypeScript)
│   ├── src/
│   │   ├── index.tsx           # React entry point
│   │   ├── App.tsx             # Route definitions
│   │   ├── components/         # Shared UI components
│   │   ├── contexts/           # React Context (auth state)
│   │   ├── pages/              # Page components
│   │   ├── services/           # Axios HTTP client
│   │   └── utils/              # Cookie helpers
│   └── public/
│       └── index.html          # HTML shell
├── docs/                       # Architecture and design documentation
└── sample-data/                # Example CSV files for testing
```

---

## Backend — File-by-File Breakdown

### Entry Point

| File | Purpose |
|------|---------|
| `backend/src/server.ts` | Express app setup. Registers all middleware (helmet, cors, rate-limit, body-parser), mounts all route groups under `/api/*`, defines `/health` and `/api/bootstrap` endpoints, handles graceful shutdown on SIGTERM/SIGINT. |

### Database Layer

| File | Purpose |
|------|---------|
| `backend/prisma/schema.prisma` | Defines 5 models: `User`, `Store`, `PricingRecord`, `AuditLog`, `UploadHistory`. Includes indexes, unique constraints (storeId+sku+date), and foreign key relationships. SQLite datasource. |
| `backend/prisma/seed.ts` | Seeds 5 stores (STORE001–STORE005), 1 admin user, 6 store manager users, and 25 sample pricing records (5 products × 5 stores). Uses `upsert` for idempotency. |
| `backend/prisma/migrations/` | Contains the initial migration SQL generated by `prisma migrate dev`. |
| `backend/src/utils/prisma.ts` | Exports a singleton `PrismaClient` instance. Logs warnings and errors in development mode. |

### Controllers

| File | Handles | Endpoints | Key Logic |
|------|---------|-----------|-----------|
| `backend/src/controllers/auth.controller.ts` | Authentication | `POST /api/auth/login`, `POST /api/auth/register`, `POST /api/auth/refresh` | Password hashing with bcrypt (10 rounds), JWT generation (24h access + 7d refresh), user lookup and validation. |
| `backend/src/controllers/store.controller.ts` | Store queries | `GET /api/stores`, `GET /api/stores/:id` | Lists stores with optional country/active filters. Store managers are scoped to their own store only. |
| `backend/src/controllers/pricing.controller.ts` | Pricing CRUD | `GET /api/pricing`, `GET /api/pricing/:id`, `PUT /api/pricing/:id`, `DELETE /api/pricing/:id` | Paginated search with filters (store, sku, product, date range, price range). Store-scoped access for managers. Audit logging on update/delete. Admin-only delete. |
| `backend/src/controllers/upload.controller.ts` | CSV upload | `POST /api/upload/csv`, `GET /api/upload/history`, `GET /api/upload/:id/status` | Async CSV processing with streaming parser. Multi-format date parsing (YYYY-MM-DD, DD/MM/YYYY, DD-MM-YYYY). Batch upsert in transactions of 500. Store ID resolution in single query. File cleanup after processing. |
| `backend/src/controllers/ai.controller.ts` | AI features | `POST /api/ai/search`, `GET /api/ai/insights`, `POST /api/ai/validate-csv`, `POST /api/ai/report` | Delegates to `ai.service.ts`. Validates query length (max 500 chars) and row count (max 5000). Respects role-based store scoping. |
